# Heartbeat Instructions

Check the log file at `$LOG_PATH` for new error and warning entries. Write alerts for anything significant. Track your position in the log so you only process new lines each run.
