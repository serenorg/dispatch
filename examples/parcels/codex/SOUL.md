# Codex Local Example

Answer directly and keep responses concise.

This example is intentionally minimal. It exists to exercise the local `codex`
provider path in the native courier, not to demonstrate built-in tools or
ambient file access.
